YaMDB API

API для проекта YaMDB — базы данных произведений (фильмы, книги, музыка) с возможностью оставлять отзывы, ставить оценки и управлять контентом через роли пользователей.

Репозиторий: https://github.com/AsBadAsUsual/api-yamdb

🛠 Технологии

Python 3.12

Django

Django REST Framework

Postgres / SQLite (указать вашу БД)

JWT для авторизации

🚀 Установка и запуск

Клонируем репозиторий:

git clone https://github.com/AsBadAsUsual/api-yamdb.git
cd api-yamdb


Создаём виртуальное окружение и активируем:

python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows


Устанавливаем зависимости:

pip install -r requirements.txt


Настраиваем .env или переменные окружения (пример):

DEBUG=True
SECRET_KEY=your_secret_key
DATABASE_URL=postgres://user:pass@localhost:5432/dbname


Выполняем миграции:

python manage.py migrate


Создаём суперпользователя (админ):

python manage.py createsuperuser


Запускаем сервер:

python manage.py runserver

🧾 Использование API
Регистрация пользователя

POST /api/v1/auth/signup/

Параметры:

{
  "email": "user@example.com",
  "username": "user123"
}


В ответ придёт confirmation_code на email.

Получение токена

POST /api/v1/auth/token/

Параметры:

{
  "username": "user123",
  "confirmation_code": "123456"
}


В ответ JWT-токен.

Работа с профилем

GET /api/v1/users/me/ — получить данные своего профиля

PATCH /api/v1/users/me/ — изменить данные своего профиля

Пример cURL запроса:

curl -H "Authorization: Bearer <token>" http://localhost:8000/api/v1/users/me/

Работа с пользователями (для администратора)

GET /api/v1/users/ — список всех пользователей

POST /api/v1/users/ — создать пользователя

GET /api/v1/users/<username>/ — получить пользователя

PATCH /api/v1/users/<username>/ — изменить пользователя

DELETE /api/v1/users/<username>/ — удалить пользователя

👥 Роли пользователей

Аноним (Anonymous) — просмотр произведений и отзывов

Аутентифицированный пользователь — публикация отзывов, оценка произведений, редактирование своих данных

Модератор — редактирование/удаление любых отзывов и комментариев

Администратор — полный доступ к проекту, управление пользователями, категориями и жанрами

Суперпользователь Django — всегда администратор

🧪 Тестирование

Запуск тестов:

pytest

📁 Структура проекта
api-yamdb/
├── api/
│   ├── serializers.py
│   ├── views.py
├── users/
│   ├── models.py
│   ├── serializers.py
├── manage.py
├── requirements.txt
├── .env
